apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: development
  name: mongo-development-pvc

spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 80Mi